# ~/.tmuxinator/whenufree.yml

name: whenufree
root: ~/p/whenufree

windows:
  - default:
    - export TERM=xterm
  - vim:
    - export TERM=xterm-256color
    - vim .
  - tests:
    - export TERM=xterm
    - rails s
